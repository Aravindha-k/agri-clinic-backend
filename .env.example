# ============================================================
# Agri Clinic — Environment (copy to .env for local dev only)
# NEVER commit .env to version control.
# ============================================================

# ------ Environment mode ------
# local | production | render | staging
APP_ENV=local

# ------ Core Django (required on Render) ------
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=True
LOG_LEVEL=INFO
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ------ CORS ------
CORS_ALLOW_ALL_ORIGINS=true
CORS_ALLOWED_ORIGINS=http://localhost:5173

# ------ Database ------
# Local: leave empty to use sqlite (db.sqlite3). Do not point at production DB.
DATABASE_URL=
# Render (set manually in Dashboard — not Blueprint-linked):
# DATABASE_URL=postgresql://user:pass@dpg-XXXXX-a/dbname
# RENDER_POSTGRES_HOST_SUFFIX=singapore-postgres.render.com
# DB_SSL_REQUIRE=false
# Do NOT use retired host dpg-d7ckj7dckfvc739s0frg-a

# ------ Redis (optional — falls back to in-memory cache) ------
REDIS_URL=
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# ------ Media storage ------
USE_S3=false
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_S3_REGION_NAME=ap-south-1

# ------ Production example (Render web service) ------
# Copy values to Render Dashboard → Environment (see render.production.env locally).
# APP_ENV=production
# DEBUG=False
# SECRET_KEY=<generate-50+-char-random-string>
# DATABASE_URL=postgresql://USER:PASS@dpg-XXXXX-a/DATABASE
# RENDER_POSTGRES_HOST_SUFFIX=singapore-postgres.render.com
# DB_SSL_REQUIRE=false
# ALLOWED_HOSTS=agri-clinic-backend.onrender.com,.onrender.com
# CSRF_TRUSTED_ORIGINS=https://agri-clinic-backend.onrender.com
# CORS_ALLOW_ALL_ORIGINS=false
# CORS_ALLOWED_ORIGINS=https://agri-clinic-frontend.onrender.com
# USE_S3=false
# LOG_LEVEL=INFO
